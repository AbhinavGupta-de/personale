services:
  postgres:
    image: postgres:17
    container_name: personale-postgres
    environment:
      POSTGRES_DB: personale
      POSTGRES_USER: personale
      POSTGRES_PASSWORD: personale
    ports:
      - "5432:5432"
    volumes:
      - personale-data:/var/lib/postgresql/data

volumes:
  personale-data:
